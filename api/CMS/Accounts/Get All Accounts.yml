info:
  name: Get All Accounts
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
